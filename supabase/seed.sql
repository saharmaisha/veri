-- Seed data is handled by the mock layer in the application.
-- This file is a placeholder for Supabase CLI compatibility.
-- To seed data for a real Supabase instance, run the app in mock mode
-- and use the UI to generate test data, or insert rows manually.

-- Example: insert a test user preference row (requires a valid user ID)
-- insert into public.user_preferences (user_id, default_budget_max, exclude_luxury)
-- values ('your-user-uuid', 150.00, true);
